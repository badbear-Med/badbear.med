(() => {"use strict";const temas=Array.isArray(window.BADBEAR_MI_TEMAS)?window.BADBEAR_MI_TEMAS:[];const grid=document.getElementById("mi-temas"),buscar=document.getElementById("mi-buscar"),filtro=document.getElementById("mi-filtro"),total=document.getElementById("mi-total-temas"),areas=document.getElementById("mi-total-areas"),comp=document.getElementById("mi-completados"),pct=document.getElementById("mi-porcentaje");const norm=s=>String(s||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();const leidos=new Set(JSON.parse(localStorage.getItem("badbear_mi_leidos")||"[]"));if(total)total.textContent=temas.length;if(areas)areas.textContent=new Set(temas.map(t=>t.area)).size;if(comp)comp.textContent=leidos.size;if(pct)pct.textContent=`${temas.length?Math.round(leidos.size/temas.length*100):0}%`;[...new Set(temas.map(t=>t.area))].sort().forEach(a=>{const o=document.createElement("option");o.value=a;o.textContent=a;filtro?.appendChild(o)});function render(){if(!grid)return;const q=norm(buscar?.value),a=filtro?.value||"todos";const arr=temas.filter(t=>(!q||norm(`${t.titulo} ${t.descripcion} ${t.area}`).includes(q))&&(a==="todos"||t.area===a));grid.innerHTML="";arr.forEach(t=>{const x=document.createElement("a");x.className="mi-topic";x.href=`tema.html?t=${encodeURIComponent(t.id)}`;x.innerHTML=`<div class="mi-topic-top"><div class="mi-topic-icon">${t.icono}</div><span class="mi-topic-area">${t.area}</span></div><h3>${t.titulo}</h3><p>${t.descripcion}</p><footer>${leidos.has(t.id)?"✓ Revisado":"Abrir tema →"}</footer>`;grid.appendChild(x)});if(!arr.length){grid.innerHTML='<div style="grid-column:1/-1;padding:30px;text-align:center;background:#fff;border:1px dashed #cbd5e1;border-radius:16px;color:#64748b">No hay temas que coincidan con la búsqueda.</div>'}}buscar?.addEventListener("input",render);filtro?.addEventListener("change",render);render()})();